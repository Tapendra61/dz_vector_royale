#include "core/event_bus.h"

// Header-only impl for now; this TU exists so vector_core has at least one
// source that pulls the header into the build for compile-checking.
namespace vector::core {}
